# API origin for fetch(). Empty = same origin / Vite dev proxy → :8000
# VITE_API_URL=http://localhost:8000

# Use only bundled demo assets (sample.tf, cached JSON) — no live /analyze
# VITE_DEMO_MODE=true

# Base path for static hosting (e.g. GitHub Pages)
# VITE_BASE=/
