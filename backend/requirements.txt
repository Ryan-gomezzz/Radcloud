fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.0.0
boto3>=1.35.0

# Auth (bcrypt directly — passlib breaks on bcrypt>=4.1, unmaintained)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# RAG (in-memory FAISS, no OpenSearch cost)
faiss-cpu>=1.8.0
numpy>=1.26.0
tiktoken>=0.7.0

# SSE streaming
sse-starlette>=2.1.0

# GCP connectivity (optional, lazy-imported)
google-cloud-asset>=3.25.0
google-cloud-billing>=1.13.0
google-auth>=2.30.0
